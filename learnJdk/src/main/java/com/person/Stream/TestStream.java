package com.person.Stream;

/**
 * Created by xujf on 2017/12/6.
 */
public class TestStream {

    //    (Map)idList.stream().distinct().collect(Collectors.toMap((x) -> {
    //        return x;
    //    }, (x) -> {
    //        return Permissions.READ_WRITE;
    //    }));
}
